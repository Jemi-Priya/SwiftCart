import { makeStyles } from "tss-react/mui";
export const useStyles = makeStyles()({
  checkoutButton: {
    cursor: "pointer",
  },
  loginButton: {
    cursor: "pointer",
    backgroundColor: "white",
    padding: "5px",
  },
});
