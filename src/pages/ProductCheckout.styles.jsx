import { makeStyles } from 'tss-react/mui'
export const useStyles = makeStyles()({
	emptyCartImage: {
		objectFit: "contain",
	},
	emptyCart: {
		display: "flex",
		justifyContent: "center",
		alignItems: "center",
	},
});